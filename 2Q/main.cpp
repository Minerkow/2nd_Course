#include "2Q.h"
#include "TestGenerator.h"

int main() {
    TestRandom(10);
 /*   Cache2Q_t<int> cache(CACHE_SIZE);
    for (int i = 0 ; i < 1000; ++i) {
        cache.check(i % 2);
    }*/
    //cache.print_hit();
    return 0;
}
